[phases.install]
cmds = ["pip install fastapi uvicorn pandas numpy scikit-learn requests python-dotenv geopandas shapely reportlab joblib"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
